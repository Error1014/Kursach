<Window x:Class="Kursach.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"

        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Kursach"
        mc:Ignorable="d"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="MainWindow" Height="600" Width="800" Loaded="Window_Loaded">
    <Window.Resources>
        <local:SpeedHelp2DataSet x:Key="speedHelp2DataSet"/>
        <CollectionViewSource x:Key="userViewSource" Source="{Binding User, Source={StaticResource speedHelp2DataSet}}"/>
    </Window.Resources>
    <Grid DataContext="{StaticResource userViewSource}">
        <StackPanel VerticalAlignment="Center">
            <TextBox x:Name="Login" FontSize="20" materialDesign:HintAssist.Hint="Логин" Style="{StaticResource MaterialDesignFilledTextBox}" Width="338" Margin="20"/>
            <PasswordBox x:Name="Pass" FontSize="20" materialDesign:HintAssist.Hint="Пароль" Style="{StaticResource MaterialDesignFilledPasswordBox}" Width="338" Margin="20"/>
            <Button MaxWidth="200" Content="Войти" Margin="20" Click="Autorization"/>
            <Button MaxWidth="200" Content="Войти по быстрому" Margin="20" Click="Button_Click"/>
        </StackPanel>
    </Grid>
</Window>
    
