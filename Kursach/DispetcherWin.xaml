<Window x:Class="Kursach.DispetcherWin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Kursach"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="DispetcherWin"  Height="800" Width="1500" WindowState="Maximized">
    <Grid>
        <Grid Background="AliceBlue">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Background="#FF9AE2FF" Grid.Column="0" >
                <Button Content="Добывить вызов" Margin="0,10,0,5" Click="AddVizov"/>
                <Button Content="Очередь вызовов" Margin="0,10,0,5" Click="ListVizov"/>
                <Button Content="Активные вызовы" Margin="0,10,0,5" Click="ListActivVizov"/>
                <Button Content="Выход" Margin="0,10,0,5" Click="Exit" Cursor="Hand" Background="#FFF35621"/>
            </StackPanel>
            <Frame Background="White" x:Name="DispetcherFrame" NavigationUIVisibility="Hidden" Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="1"/>
        </Grid>
    </Grid>
</Window>
